# Ontology migrations are applied at most once each; applied ids are tracked
# in .contextgraph/applied.lock. `ingest_ontology` projects ontology/*,
# master_data/*, policies/* and processes/* into the graph as meta.* nodes.

id: 0001_initial
description: |
  Initial ingest of the product-master (better-for-you snacks) ontology.
  Loads 20 entity types, 29 relationships, 15 vocabularies, 3 hierarchies,
  8 KPIs, 8 constraints, and 2 processes.
created_at: 2026-07-11
operations:
  - kind: ingest_ontology
